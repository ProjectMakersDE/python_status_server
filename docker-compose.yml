version: '3.8'

services:
  web:
    build: .
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      - mongodb
    env_file:
      - .env
    restart: unless-stopped